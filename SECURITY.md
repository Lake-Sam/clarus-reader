# Security policy

Please report vulnerabilities privately through GitHub's security advisory feature rather than a public issue. Include affected versions, reproduction steps, and potential impact.

Clarus supports the current release only. API keys are stored with Windows Credential Manager; application settings and chat history do not contain keys. Treat PDFs as untrusted input and avoid changes that execute document content or treat it as model instructions.
